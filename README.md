# 🐾 Rescue Paw

##  Backend (Django REST API) : Installation Initiale (Première fois)
Situé dans le dossier '/paw_backend'
Si vous venez de cloner le projet, suivez ces étapes pour configurer le backend :

1. **Entrer dans le dossier :**
   'cd paw_backend'

2. **Créer l'environnement virtuel :**
   'python -m venv venv'

3. **Activer l'environnement :**
   - Linux/Mac : 'source venv/bin activate'
   - Windows : 'venv\Scripts\activate'

4. **Installer les dépendances :**
   'pip install -r requirements.txt'

## Lancement Quotidien

**Pour lancer le serveur:**
1. 'cd paw_backend'
2. 'source venv/bin/activate'
3. 'python manage.py runserver'

## Frontend (React/Vue)
Situé dans le dossier '/paw_frontend'
(Instructions à remplir par Yane)

## Connexion API
L'API est accessible sur : 'http://127.0.0.1:8000/api/'